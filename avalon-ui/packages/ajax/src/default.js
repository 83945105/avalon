import Ajax from "./ajax.js";

export default class Default extends Ajax {
    get(url, params, options) {
        alert('未实现');
    }

    post(url, params, options) {
        alert('未实现');
    }

    put(url, params, options) {
        alert('未实现');
    }

    delete(url, params, options) {
        alert('未实现');
    }

    download(fileName, url, params, options) {
        alert('未实现');
    }
}